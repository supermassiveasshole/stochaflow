<!-- Generated by tools/generate_config_reference.py; do not edit. -->
# 配置字段参考

本页由运行时 dataclass、Registry、argparse 与中文元数据确定性生成。
修改配置接口后必须运行 `uv run python tools/generate_config_reference.py`。

字段路径中的 `[]` 表示列表中的每个元素。`params` 是 Registry 组件的
构造参数容器；允许的参数见本页后半部分的组件索引。

## `experiment`

(config-field-path-experiment)=
### `experiment`

实验标识、随机性和输出位置。

- 类型：`mapping`
- 必填：是

(config-field-path-experiment-name)=
### `experiment.name`

人类可读的实验名称，用于日志 run name。

- 类型：`str`
- 必填：是

(config-field-path-experiment-seed)=
### `experiment.seed`

数据划分、采样器、训练与采样的基础随机种子。

- 类型：`int`
- 必填：否
- 默认值：`42`

(config-field-path-experiment-output-dir)=
### `experiment.output_dir`

运行目录根路径；训练会在其下创建时间戳子目录。

- 类型：`str`
- 必填：否
- 默认值：`outputs/default`
- CLI 覆盖：`train --output-dir`

(config-field-path-experiment-exp-id)=
### `experiment.exp_id`

运行实例标识；通常由 runner 在创建时间戳目录后写入。

- 类型：`str | null`
- 必填：否
- 默认值：`null`

## `data`

(config-field-path-data)=
### `data`

由 data_builders Registry 构建的一次训练运行数据入口。

- 类型：`mapping`
- 必填：是

(config-field-path-data-name)=
### `data.name`

data_builders Registry 名称。

- 类型：`str`
- 必填：是

(config-field-path-data-params)=
### `data.params`

DataBuilder 完整拥有的参数；核心不解释 Dataset、split、sampler、collate 或 batch 语义。

- 类型：`mapping[str, any]`
- 必填：否
- 默认值：`{}`

## `model`

(config-field-path-model)=
### `model`

由 models Registry 构建的去噪模型。

- 类型：`mapping`
- 必填：是

(config-field-path-model-name)=
### `model.name`

models Registry 名称。

- 类型：`str`
- 必填：是

(config-field-path-model-params)=
### `model.params`

模型构造参数；内置参数见 Registry 组件索引。

- 类型：`mapping[str, any]`
- 必填：否
- 默认值：`{}`

## `diffusion`

(config-field-path-diffusion)=
### `diffusion`

训练扩散过程及其前向噪声路径。

- 类型：`mapping`
- 必填：是

(config-field-path-diffusion-name)=
### `diffusion.name`

diffusions Registry 名称；也决定默认训练后采样器。

- 类型：`str`
- 必填：是

(config-field-path-diffusion-noise-schedule)=
### `diffusion.noise_schedule`

前向噪声 schedule 的 Registry 声明。

- 类型：`mapping`
- 必填：是

(config-field-path-diffusion-noise-schedule-name)=
### `diffusion.noise_schedule.name`

noise_schedules Registry 名称。

- 类型：`str`
- 必填：是

(config-field-path-diffusion-noise-schedule-params)=
### `diffusion.noise_schedule.params`

噪声 schedule 构造参数。

- 类型：`mapping[str, any]`
- 必填：否
- 默认值：`{}`

(config-field-path-diffusion-params)=
### `diffusion.params`

扩散过程构造参数；model 与 noise_schedule 由运行时注入。

- 类型：`mapping[str, any]`
- 必填：否
- 默认值：`{}`

## `objective`

(config-field-path-objective)=
### `objective`

训练目标函数的 Registry 声明。

- 类型：`mapping`
- 必填：是

(config-field-path-objective-name)=
### `objective.name`

objectives Registry 名称。

- 类型：`str`
- 必填：是

(config-field-path-objective-params)=
### `objective.params`

目标函数构造参数。

- 类型：`mapping[str, any]`
- 必填：否
- 默认值：`{}`

## `extensions`

(config-field-path-extensions)=
### `extensions`

在配置校验、训练组件构建和 checkpoint 采样前加载的通用 Registry 扩展。

- 类型：`mapping`
- 必填：否
- 默认值：`{modules: []}`

(config-field-path-extensions-modules)=
### `extensions.modules`

按声明顺序幂等导入的 Python 扩展模块；模块导入应执行 Registry 装饰器。

- 类型：`list[str]`
- 必填：否
- 默认值：`[]`
- 约束：每项是非空 import path。

## `optimizer`

(config-field-path-optimizer)=
### `optimizer`

优化器 Registry 声明。

- 类型：`mapping`
- 必填：否
- 默认值：`{name: adam, params: {betas: [0.9, 0.999], eps: 1.0e-08, lr: 0.0002, weight_decay: 0.0}}`

(config-field-path-optimizer-name)=
### `optimizer.name`

optimizers Registry 名称。

- 类型：`str`
- 必填：否
- 默认值：`adam`

(config-field-path-optimizer-params)=
### `optimizer.params`

透传给优化器的参数；模型 parameters 由运行时注入。

- 类型：`mapping[str, any]`
- 必填：否
- 默认值：`{betas: [0.9, 0.999], eps: 1.0e-08, lr: 0.0002, weight_decay: 0.0}`

## `lr_scheduler`

(config-field-path-lr-scheduler)=
### `lr_scheduler`

可选学习率调度器及更新周期。

- 类型：`mapping`
- 必填：否
- 默认值：`{interval: step, name: null, params: {}}`

(config-field-path-lr-scheduler-name)=
### `lr_scheduler.name`

lr_schedulers Registry 名称；null 禁用调度器。

- 类型：`str | null`
- 必填：否
- 默认值：`null`

(config-field-path-lr-scheduler-interval)=
### `lr_scheduler.interval`

在每个 optimizer step 后或每个 epoch 后推进调度器。

- 类型：`str`
- 必填：否
- 默认值：`step`
- 约束：启用时为 step 或 epoch。

(config-field-path-lr-scheduler-params)=
### `lr_scheduler.params`

调度器构造参数；optimizer 由运行时注入。

- 类型：`mapping[str, any]`
- 必填：否
- 默认值：`{}`

## `ema`

(config-field-path-ema)=
### `ema`

模型参数的指数移动平均策略。

- 类型：`mapping`
- 必填：否
- 默认值：`{decay: 0.9999, enabled: false, update_after_step: 0, update_every: 1, use_for_sampling: true}`

(config-field-path-ema-enabled)=
### `ema.enabled`

是否创建并更新 EMA 权重。

- 类型：`bool`
- 必填：否
- 默认值：`false`

(config-field-path-ema-decay)=
### `ema.decay`

历史 EMA 权重的保留系数。

- 类型：`float`
- 必填：否
- 默认值：`0.9999`
- 约束：大于等于零且小于一。

(config-field-path-ema-update-after-step)=
### `ema.update_after_step`

在全局 step 达到该值后才开始更新 EMA。

- 类型：`int`
- 必填：否
- 默认值：`0`
- 约束：非负整数。

(config-field-path-ema-update-every)=
### `ema.update_every`

每隔多少个 optimizer step 更新一次 EMA。

- 类型：`int`
- 必填：否
- 默认值：`1`
- 约束：正整数。

(config-field-path-ema-use-for-sampling)=
### `ema.use_for_sampling`

采样和诊断 artifact 是否优先使用 EMA 权重。

- 类型：`bool`
- 必填：否
- 默认值：`true`

## `sampling`

(config-field-path-sampling)=
### `sampling`

独立采样和训练结束验收采样的默认设置。

- 类型：`mapping`
- 必填：否
- 默认值：`{batch_size: 16, debug: {trajectory: {enabled: false, params: {}}}, num_samples: 16, sampler: null, seed: null, shape: null, writers: [{name: tensor, params: {}}]}`

(config-field-path-sampling-sampler)=
### `sampling.sampler`

可选采样器覆盖；null 时使用 diffusion.name 与 diffusion.params。

- 类型：`mapping | null`
- 必填：否
- 默认值：`null`

(config-field-path-sampling-sampler-name)=
### `sampling.sampler.name`

作为采样器使用的 diffusions Registry 名称。

- 类型：`str`
- 必填：是
- CLI 覆盖：`sample --sampler`

(config-field-path-sampling-sampler-params)=
### `sampling.sampler.params`

采样器构造参数；CLI --sampler-param 在其后覆盖。

- 类型：`mapping[str, any]`
- 必填：否
- 默认值：`{}`
- CLI 覆盖：`sample --sampler-param`

(config-field-path-sampling-shape)=
### `sampling.shape`

单个生成 state 的形状，不含 batch 维；null 允许不需要固定 shape 的自定义运行时。

- 类型：`list[int] | null`
- 必填：否
- 默认值：`null`
- 约束：非 null 时为非空正整数列表。

(config-field-path-sampling-num-samples)=
### `sampling.num_samples`

一次采样生成的总样本数。

- 类型：`int`
- 必填：否
- 默认值：`16`
- 约束：正整数。

(config-field-path-sampling-batch-size)=
### `sampling.batch_size`

采样时每批样本数，与 DataBuilder 独立。

- 类型：`int`
- 必填：否
- 默认值：`16`
- 约束：正整数。

(config-field-path-sampling-seed)=
### `sampling.seed`

采样专用随机种子；null 使用 experiment.seed。

- 类型：`int | null`
- 必填：否
- 默认值：`null`

(config-field-path-sampling-writers)=
### `sampling.writers`

按声明顺序运行的 sampling_artifact_writers 组件。

- 类型：`list[mapping]`
- 必填：否
- 默认值：`[{name: tensor, params: {}}]`
- 约束：至少一个。

(config-field-path-sampling-writers-item-name)=
### `sampling.writers[].name`

sampling_artifact_writers Registry 名称。

- 类型：`str`
- 必填：是

(config-field-path-sampling-writers-item-params)=
### `sampling.writers[].params`

writer 构造参数；图像网格与 GIF 参数属于 image writer。

- 类型：`mapping[str, any]`
- 必填：否
- 默认值：`{}`

(config-field-path-sampling-debug)=
### `sampling.debug`

仅用于调试的采样 artifact 设置。

- 类型：`mapping`
- 必填：否
- 默认值：`{trajectory: {enabled: false, params: {}}}`

(config-field-path-sampling-debug-trajectory)=
### `sampling.debug.trajectory`

反向过程 trajectory 的输出设置。

- 类型：`mapping`
- 必填：否
- 默认值：`{enabled: false, params: {}}`

(config-field-path-sampling-debug-trajectory-enabled)=
### `sampling.debug.trajectory.enabled`

是否保存原始 trajectory、静态网格和 GIF。

- 类型：`bool`
- 必填：否
- 默认值：`false`

(config-field-path-sampling-debug-trajectory-params)=
### `sampling.debug.trajectory.params`

透传给采样器 trajectory 方法的参数。

- 类型：`mapping[str, any]`
- 必填：否
- 默认值：`{}`

## `diagnostics`

(config-field-path-diagnostics)=
### `diagnostics`

训练期间按声明顺序实例化的 diagnostics Registry 组件列表。

- 类型：`list[mapping]`
- 必填：否
- 默认值：`[]`

(config-field-path-diagnostics-item-name)=
### `diagnostics[].name`

diagnostics Registry 名称。

- 类型：`str`
- 必填：是

(config-field-path-diagnostics-item-params)=
### `diagnostics[].params`

diagnostic 构造参数；logger、output_dir 与 sample_shape 由运行时注入。

- 类型：`mapping[str, any]`
- 必填：否
- 默认值：`{}`

## `trainer`

(config-field-path-trainer)=
### `trainer`

训练循环、设备、梯度和早停配置。

- 类型：`mapping`
- 必填：否
- 默认值：`{device: cpu, early_stopping: {enabled: false, min_delta: 0.0, mode: min, monitor: valid_loss, patience: 10}, max_grad_norm: null, num_epochs: 1, show_progress: true}`

(config-field-path-trainer-num-epochs)=
### `trainer.num_epochs`

每个独立 run 的最大训练 epoch 数。

- 类型：`int`
- 必填：否
- 默认值：`1`
- 约束：正整数。
- CLI 覆盖：`train --epochs`

(config-field-path-trainer-device)=
### `trainer.device`

Torch 设备字符串；auto 依次选择 CUDA、MPS、CPU。

- 类型：`str`
- 必填：否
- 默认值：`cpu`
- CLI 覆盖：`train --device`

(config-field-path-trainer-max-grad-norm)=
### `trainer.max_grad_norm`

梯度范数裁剪阈值；null 禁用裁剪。

- 类型：`float | null`
- 必填：否
- 默认值：`null`
- 约束：非 null 时为正数。

(config-field-path-trainer-show-progress)=
### `trainer.show_progress`

是否显示 Rich 进度条。

- 类型：`bool`
- 必填：否
- 默认值：`true`
- CLI 覆盖：`train --no-progress`

(config-field-path-trainer-early-stopping)=
### `trainer.early_stopping`

基于 epoch 指标的提前停止策略。

- 类型：`mapping`
- 必填：否
- 默认值：`{enabled: false, min_delta: 0.0, mode: min, monitor: valid_loss, patience: 10}`

(config-field-path-trainer-early-stopping-enabled)=
### `trainer.early_stopping.enabled`

是否启用提前停止。

- 类型：`bool`
- 必填：否
- 默认值：`false`

(config-field-path-trainer-early-stopping-monitor)=
### `trainer.early_stopping.monitor`

需要监控的 epoch 指标名，例如 valid_loss 或 train_loss。

- 类型：`str`
- 必填：否
- 默认值：`valid_loss`

(config-field-path-trainer-early-stopping-mode)=
### `trainer.early_stopping.mode`

min 表示越小越好，max 表示越大越好。

- 类型：`str`
- 必填：否
- 默认值：`min`
- 约束：min 或 max。

(config-field-path-trainer-early-stopping-patience)=
### `trainer.early_stopping.patience`

指标无足够改善时容忍的 epoch 数。

- 类型：`int`
- 必填：否
- 默认值：`10`
- 约束：正整数。

(config-field-path-trainer-early-stopping-min-delta)=
### `trainer.early_stopping.min_delta`

被视为改善所需的最小绝对差值。

- 类型：`float`
- 必填：否
- 默认值：`0.0`
- 约束：非负数。

## `logging`

(config-field-path-logging)=
### `logging`

指标记录频率、后端与 torch 内部日志配置。

- 类型：`mapping`
- 必填：否
- 默认值：`{backends: [{name: local, params: {}}], log_every: 100, torch_logs: {}}`

(config-field-path-logging-log-every)=
### `logging.log_every`

每隔多少个训练 step 写一次 step 指标。

- 类型：`int`
- 必填：否
- 默认值：`100`
- 约束：正整数。

(config-field-path-logging-backends)=
### `logging.backends`

按声明顺序组合的 logger Registry 组件。

- 类型：`list[mapping]`
- 必填：否
- 默认值：`[{name: local, params: {}}]`
- 约束：至少一个。

(config-field-path-logging-backends-item-name)=
### `logging.backends[].name`

loggers Registry 名称。

- 类型：`str`
- 必填：是

(config-field-path-logging-backends-item-params)=
### `logging.backends[].params`

logger 构造参数；output_dir 与 run_name 由运行时注入。

- 类型：`mapping[str, any]`
- 必填：否
- 默认值：`{}`

(config-field-path-logging-torch-logs)=
### `logging.torch_logs`

传给 torch._logging.set_logs 的关键字；依赖当前 PyTorch 版本。

- 类型：`mapping[str, any]`
- 必填：否
- 默认值：`{}`

## `artifacts`

(config-field-path-artifacts)=
### `artifacts`

训练 artifact 的保存频率。

- 类型：`mapping`
- 必填：否
- 默认值：`{checkpoint_every: 1}`

(config-field-path-artifacts-checkpoint-every)=
### `artifacts.checkpoint_every`

每隔多少个 epoch 保存编号 checkpoint；best 与 latest 仍按各自规则更新。

- 类型：`int`
- 必填：否
- 默认值：`1`
- 约束：正整数。

## Registry 组件索引

(config-registry-name-models)=
### `models`

构建可训练的去噪网络。

- 基类/契约：`torch.nn.Module`
- 配置位置：`model.name / model.params`

(config-component-models-unet)=
#### `unet`

带时间嵌入、残差块和可选注意力的图像 UNet。

| 参数 | 含义与约束 |
| --- | --- |
| `in_channels` | 输入 state 的通道数；由所用模型和训练策略共同约定。 |
| `out_channels` | 输出预测的通道数；由所用模型和训练策略共同约定。 |
| `base_channels` | 第一层特征通道数。 |
| `channel_multipliers` | 每个分辨率层相对 base_channels 的通道倍数；长度决定下采样深度。 |
| `num_res_blocks` | 每个分辨率层的残差块数量。 |
| `time_embedding_dim` | 正弦时间嵌入和 MLP 的基础维度。 |
| `dropout` | 残差块 dropout 概率。 |
| `attention_levels` | 启用空间注意力的零起始分辨率层索引；null 禁用。 |
| `attention_heads` | 每个注意力块的 head 数。 |

(config-registry-name-data-builders)=
### `data_builders`

直接组装 Dataset、划分、sampler、collate 和 loader，并返回单次运行的 DataLoaders。

- 基类/契约：`stochaflow.data.DataBuilder`
- 配置位置：`data.name / data.params`

(config-component-data_builders-image)=
#### `image`

单一 torchvision 或本地图像来源的标准图像 recipe。

运行时注入（不得在 YAML 中覆盖）：`context`。

无组件级配置参数。

(config-component-data_builders-multi-resolution-image)=
#### `multi_resolution_image`

多源加权混合、resolution bucket 与动态像素预算 batch 的图像 recipe。

运行时注入（不得在 YAML 中覆盖）：`context`。

无组件级配置参数。

(config-component-data_builders-super-resolution)=
#### `super_resolution`

从 HR 在线生成 LR 或读取配对目录的图像超分 recipe。

运行时注入（不得在 YAML 中覆盖）：`context`。

无组件级配置参数。

(config-registry-name-sampling-artifact-writers)=
### `sampling_artifact_writers`

将生成 batch 写成模态相关 artifact，并返回已存在的文件路径。

- 基类/契约：`stochaflow.sampling.SamplingArtifactWriter`
- 配置位置：`sampling.writers[].name / sampling.writers[].params`

(config-component-sampling_artifact_writers-tensor)=
#### `tensor`

保存 samples.pt 和可选 trajectory.pt。

无组件级配置参数。

(config-component-sampling_artifact_writers-image)=
#### `image`

校验 NCHW 图像并保存 PNG、trajectory grid 和 GIF。

| 参数 | 含义与约束 |
| --- | --- |
| `grid_nrow` | 图像网格每行样本数。 |
| `gif_fps` | trajectory GIF 帧率。 |
| `denormalize` | 写图前是否将负一到一映射为零到一。 |

(config-registry-name-noise-schedules)=
### `noise_schedules`

定义前向噪声路径以及离散 VP 系数。

- 基类/契约：`stochaflow.diffusion.NoiseSchedule`
- 配置位置：`diffusion.noise_schedule.name / diffusion.noise_schedule.params`

(config-component-noise_schedules-cosine-alpha-bar)=
#### `cosine_alpha_bar`

通过余弦 alpha_bar 曲线离散化得到 beta 序列。

| 参数 | 含义与约束 |
| --- | --- |
| `num_timesteps` | 离散扩散步数；必填正整数。 |
| `s` | 余弦曲线起点偏移；默认 0.008。 |
| `max_beta` | 单步 beta 上限；默认 0.999。 |
| `dtype` | 系数 Tensor dtype；仅建议在 Python 配置中使用，YAML 保持默认。 |

(config-component-noise_schedules-linear-beta)=
#### `linear_beta`

在 beta_start 与 beta_end 之间线性插值。

| 参数 | 含义与约束 |
| --- | --- |
| `num_timesteps` | 离散扩散步数；必填正整数。 |
| `beta_start` | 第一步 beta；默认 0.0001。 |
| `beta_end` | 最后一步 beta；默认 0.02。 |
| `dtype` | 系数 Tensor dtype；仅建议在 Python 配置中使用，YAML 保持默认。 |

(config-registry-name-diffusions)=
### `diffusions`

构建训练/采样扩散过程；同一 Registry 也供 sampling.sampler 选择。

- 基类/契约：`torch.nn.Module`
- 配置位置：`diffusion.name / diffusion.params 或 sampling.sampler`

(config-component-diffusions-ddim)=
#### `ddim`

支持步数压缩和 eta 随机性的 DDIM 反向过程。

运行时注入（不得在 YAML 中覆盖）：`model`, `noise_schedule`。

| 参数 | 含义与约束 |
| --- | --- |
| `num_inference_steps` | 推理反向步数；null 使用完整 schedule。 |
| `eta` | 随机性系数；0 为确定性 DDIM。 |
| `clip_denoised` | 是否将预测的 x0 裁剪到负一到一。 |

(config-component-diffusions-ddpm)=
#### `ddpm`

标准随机 DDPM 反向过程。

运行时注入（不得在 YAML 中覆盖）：`model`, `noise_schedule`。

| 参数 | 含义与约束 |
| --- | --- |
| `clip_denoised` | 是否将预测的 x0 裁剪到负一到一。 |

(config-registry-name-objectives)=
### `objectives`

将模型输出和前向噪声目标转换为标量训练损失。

- 基类/契约：`torch.nn.Module`
- 配置位置：`objective.name / objective.params`

(config-component-objectives-ddpm-epsilon)=
#### `ddpm_epsilon`

训练模型预测前向过程注入的 epsilon 噪声。

| 参数 | 含义与约束 |
| --- | --- |
| `reduction` | MSE 聚合方式；支持 mean、sum 或 none。 |

(config-registry-name-optimizers)=
### `optimizers`

构建 PyTorch 优化器；模型 parameters 由运行时注入。

- 基类/契约：`torch.optim.Optimizer`
- 配置位置：`optimizer.name / optimizer.params`

(config-component-optimizers-adam)=
#### `adam`

PyTorch Adam 优化器。

运行时注入（不得在 YAML 中覆盖）：`params`。

> 参数由当前 PyTorch 版本透传；下表只列项目承诺的常用参数，最终以所安装 PyTorch 版本为准。

| 参数 | 含义与约束 |
| --- | --- |
| `lr` | 学习率。 |
| `betas` | 一阶和二阶矩估计的衰减系数。 |
| `eps` | 数值稳定项。 |
| `weight_decay` | L2 权重衰减。 |
| `amsgrad` | 是否使用 AMSGrad 变体。 |
| `foreach` | 是否使用 foreach 实现；null 由 PyTorch 决定。 |
| `maximize` | 是否最大化目标。 |
| `capturable` | 是否允许 CUDA graph capture。 |
| `differentiable` | optimizer step 是否参与 autograd。 |
| `fused` | 是否使用 fused 实现；null 由 PyTorch 决定。 |
| `decoupled_weight_decay` | 是否使用解耦权重衰减；依赖 PyTorch 版本。 |

(config-component-optimizers-adamw)=
#### `adamw`

使用解耦权重衰减的 PyTorch AdamW。

运行时注入（不得在 YAML 中覆盖）：`params`。

> 参数由当前 PyTorch 版本透传；下表只列项目承诺的常用参数，最终以所安装 PyTorch 版本为准。

| 参数 | 含义与约束 |
| --- | --- |
| `lr` | 学习率。 |
| `betas` | 一阶和二阶矩估计的衰减系数。 |
| `eps` | 数值稳定项。 |
| `weight_decay` | 解耦权重衰减系数。 |
| `amsgrad` | 是否使用 AMSGrad 变体。 |
| `foreach` | 是否使用 foreach 实现；null 由 PyTorch 决定。 |
| `maximize` | 是否最大化目标。 |
| `capturable` | 是否允许 CUDA graph capture。 |
| `differentiable` | optimizer step 是否参与 autograd。 |
| `fused` | 是否使用 fused 实现；null 由 PyTorch 决定。 |

(config-registry-name-lr-schedulers)=
### `lr_schedulers`

构建学习率调度器；optimizer 由运行时注入。

- 基类/契约：`torch.optim.lr_scheduler.LRScheduler 或注册 builder`
- 配置位置：`lr_scheduler.name / lr_scheduler.params`

(config-component-lr_schedulers-cosine)=
#### `cosine`

PyTorch CosineAnnealingLR。

运行时注入（不得在 YAML 中覆盖）：`optimizer`。

> 参数由当前 PyTorch 版本透传；下表只列项目承诺的常用参数，最终以所安装 PyTorch 版本为准。

| 参数 | 含义与约束 |
| --- | --- |
| `T_max` | 余弦周期的最大迭代数；auto 根据 interval 使用有效训练 epoch 数或总 step 数。 |
| `eta_min` | 最低学习率。 |
| `last_epoch` | 恢复时的最后 epoch/step 索引。 |

(config-component-lr_schedulers-exponential)=
#### `exponential`

PyTorch ExponentialLR。

运行时注入（不得在 YAML 中覆盖）：`optimizer`。

> 参数由当前 PyTorch 版本透传；下表只列项目承诺的常用参数，最终以所安装 PyTorch 版本为准。

| 参数 | 含义与约束 |
| --- | --- |
| `gamma` | 每次推进时乘到学习率上的系数。 |
| `last_epoch` | 恢复时的最后 epoch/step 索引。 |

(config-component-lr_schedulers-linear)=
#### `linear`

PyTorch LinearLR。

运行时注入（不得在 YAML 中覆盖）：`optimizer`。

> 参数由当前 PyTorch 版本透传；下表只列项目承诺的常用参数，最终以所安装 PyTorch 版本为准。

| 参数 | 含义与约束 |
| --- | --- |
| `start_factor` | 初始学习率乘数。 |
| `end_factor` | 结束学习率乘数。 |
| `total_iters` | 线性变化的推进次数。 |
| `last_epoch` | 恢复时的最后 epoch/step 索引。 |

(config-component-lr_schedulers-multistep)=
#### `multistep`

PyTorch MultiStepLR。

运行时注入（不得在 YAML 中覆盖）：`optimizer`。

> 参数由当前 PyTorch 版本透传；下表只列项目承诺的常用参数，最终以所安装 PyTorch 版本为准。

| 参数 | 含义与约束 |
| --- | --- |
| `milestones` | 应用 gamma 的递增 step/epoch 索引。 |
| `gamma` | milestone 到达时的学习率乘数。 |
| `last_epoch` | 恢复时的最后 epoch/step 索引。 |

(config-component-lr_schedulers-step)=
#### `step`

PyTorch StepLR。

运行时注入（不得在 YAML 中覆盖）：`optimizer`。

> 参数由当前 PyTorch 版本透传；下表只列项目承诺的常用参数，最终以所安装 PyTorch 版本为准。

| 参数 | 含义与约束 |
| --- | --- |
| `step_size` | 每隔多少次推进应用 gamma。 |
| `gamma` | 学习率乘数。 |
| `last_epoch` | 恢复时的最后 epoch/step 索引。 |

(config-component-lr_schedulers-warmup-cosine)=
#### `warmup_cosine`

项目内置的线性 warmup 加余弦衰减 builder。

运行时注入（不得在 YAML 中覆盖）：`optimizer`。

| 参数 | 含义与约束 |
| --- | --- |
| `warmup_steps` | 线性 warmup 的正 step 数。 |
| `total_steps` | 总 step 数；配置允许正整数或 auto。 |
| `min_lr_ratio` | 最终学习率相对基础学习率的比例，范围为零到一。 |

(config-registry-name-loggers)=
### `loggers`

记录配置、指标、文本和图像 artifact。

- 基类/契约：`stochaflow.utils.logging.ExperimentLogger`
- 配置位置：`logging.backends[].name / logging.backends[].params`

(config-component-loggers-local)=
#### `local`

写入终端、train.log 和 metrics.jsonl 的本地 logger。

运行时注入（不得在 YAML 中覆盖）：`output_dir`, `run_name`。

| 参数 | 含义与约束 |
| --- | --- |
| `console` | 是否同时输出到终端。 |
| `text_filename` | 文本日志文件名。 |
| `metrics_filename` | JSONL 指标文件名。 |
| `append` | 是否追加到已有文件；resume 时 runner 可覆盖。 |

(config-component-loggers-tensorboard)=
#### `tensorboard`

将指标和图像写入 TensorBoard event 文件。

运行时注入（不得在 YAML 中覆盖）：`output_dir`, `run_name`。

| 参数 | 含义与约束 |
| --- | --- |
| `subdir` | 运行目录内的 TensorBoard 子目录。 |

(config-component-loggers-wandb)=
#### `wandb`

将运行同步到 Weights & Biases；需要安装 wandb extra。

运行时注入（不得在 YAML 中覆盖）：`output_dir`, `run_name`。

| 参数 | 含义与约束 |
| --- | --- |
| `project` | W&B project 名称。 |
| `entity` | W&B entity；null 使用当前默认。 |
| `mode` | W&B 模式，例如 online、offline 或 disabled。 |
| `tags` | run tag 列表。 |

(config-registry-name-diagnostics)=
### `diagnostics`

训练期间按周期记录 denoiser 指标并比较多个 sampler 的质量与 artifact。

- 基类/契约：`stochaflow.training.diagnostics.TrainingDiagnostic`
- 配置位置：`diagnostics[].name / diagnostics[].params`

(config-component-diagnostics-diffusion-quality)=
#### `diffusion_quality`

通过可插拔 provider 记录 denoiser 指标，并在固定噪声下比较多个 sampler profile。

运行时注入（不得在 YAML 中覆盖）：`logger`, `output_dir`, `sample_shape`。

| 参数 | 含义与约束 |
| --- | --- |
| `samplers` | sampler profile 列表；每项声明唯一 id、Registry name、构造 params 和可选 trajectory。 |
| `modules` | 按声明顺序导入并注册第三方 diagnostic provider 的 Python module。 |
| `cadence` | step metric 与 epoch artifact 的执行周期。 |
| `sampling` | 多 profile 共用的样本数、batch size 和基础 seed。 |
| `providers` | step metric、sampler metric、denoiser artifact 与 sampler artifact provider 列表。 |
| `reference` | 可选参考指标 provider、周期、真实/生成样本数和 batch size。 |
| `use_ema` | diagnostic 推理是否优先使用 EMA 权重。 |
| `failure_policy` | 运行期错误采用 raise 或 warn；配置错误始终失败。 |

## CLI 参数索引

(config-cli-train)=
### `stochaflow train`

| 参数 | 必填 | 默认值 | 含义 |
| --- | --- | --- | --- |
| `--config` | 是 | `—` | 实验 YAML 路径。 |
| `--device` | 否 | `—` | 本次运行覆盖 trainer.device。 |
| `--output-dir` | 否 | `—` | 本次运行覆盖 experiment.output_dir。 |
| `--epochs` | 否 | `—` | 本次运行覆盖 trainer.num_epochs。 |
| `--limit-batches` | 否 | `—` | 每个 epoch 最多执行的训练 batch 数，用于 smoke/debug。 |
| `--limit-validation-batches` | 否 | `—` | 每个 epoch 最多执行的验证 batch 数。 |
| `--limit-test-batches` | 否 | `—` | 最终测试最多执行的 batch 数。 |
| `--deterministic` | 否 | `false` | 在 Torch 支持的范围内启用确定性算法。 |
| `--no-progress` | 否 | `false` | 禁用 Rich 进度条。 |
| `--resume` | 否 | `—` | 无值时恢复配置输出根下的 latest.pt；也可显式给 checkpoint 路径。 |
| `--skip-final-sample` | 否 | `false` | 跳过训练完成后的最佳 checkpoint 验收采样。 |

(config-cli-sample)=
### `stochaflow sample`

| 参数 | 必填 | 默认值 | 含义 |
| --- | --- | --- | --- |
| `--config` | 否 | `—` | 可选外部配置；其 sampling 设置覆盖 checkpoint 中的采样设置。 |
| `--checkpoint` | 否 | `—` | checkpoint 文件或运行目录；仅给 config 时自动查找最新 best.pt。 |
| `--sampler` | 否 | `—` | 覆盖采样器 Registry 名称。 |
| `--sampler-param` | 否 | `—` | KEY=VALUE 形式的可重复采样器参数覆盖，值按 YAML 解析。 |
| `--device` | 否 | `—` | 覆盖采样设备。 |
| `--output-dir` | 否 | `—` | 覆盖生成 artifact 的目录。 |
